import styled from '@emotion/styled'

export const AppBarStyle = styled.div`
	& span {
		left: 0;
		position: absolute;
	}

	position: static;

	display: flex;
	align-items: center;
	justify-content: center;
`
